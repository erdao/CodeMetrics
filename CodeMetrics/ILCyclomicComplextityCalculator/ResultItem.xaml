<UserControl x:Class="ILCyclomicComplextityCalculator.ResultItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ILCyclomicComplextityCalculator"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" x:Name="ThisUserControl" Width="700">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding ElementName=ThisUserControl,Path=MethodName}"/>
        <TextBlock Grid.Column="1" Text="{Binding ElementName=ThisUserControl,Path=ILCC}"/>
        <TextBlock Grid.Column="2" Text="{Binding ElementName=ThisUserControl,Path=PassOrNot}"/>
    </Grid>
</UserControl>
